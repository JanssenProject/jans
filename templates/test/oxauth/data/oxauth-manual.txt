1. Select all dynamicGrantTypeDefault in JSON Config

2. Add test scope with member_of claim (done in > CE 3.1.4)

3. legacyIdTokenClaims = TRUE in JSON config

4. Add `oxIncludeClaimsInIdToken` to dynamicRegistrationCustomAttributes in JSON config