1)
    "dynamicRegistrationCustomObjectClass":"oxAuthClientCustomAttributes",

2)
    "dynamicRegistrationCustomAttributes":[
        "oxAuthTrustedClient",
        "myCustomAttr1",
        "myCustomAttr2"
    ],

3. Select all dynamicGrantTypeDefault in JSON Config

4. Add test scope with member_of claim (done in > CE 3.1.4)

5. legacyIdTokenClaims = TRUE in JSON config

6. Add `oxIncludeClaimsInIdToken` to dynamicRegistrationCustomAttributes in JSON config

7. Enable UMA scripts

8. Enable few Person auth scripts

9. Fix sector identifier numbers to conform tests
