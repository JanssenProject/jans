apiVersion: couchbase.com/v2
kind: CouchbaseRoleBinding
metadata:
  name: jans-role-binding
spec:
  subjects:
  - kind: CouchbaseUser
    name: jans
  roleRef:
    kind: CouchbaseGroup
    name: jans-group
