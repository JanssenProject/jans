#!/usr/bin/env python

import json

from default import DEFAULT

info = DEFAULT.copy()

info["provider"] = {"dynamic": "https://localhost"}

info["interaction"] = [
    {
        "matches": {
            "url": "https://localhost/oxauth/login.seam"
        },
        "page-type": "login",
        "control": {
            "type": "form",
            "set": {"loginForm:username": "test_user",
                    "loginForm:password": "test"}
        }
    },
    {
        "matches": {
            "url": "https://localhost/oxauth/authorize.seam"
        },
        "page-type": "user-consent",
        "control": {
            "type": "form",
            "click": "authorizeForm:allowButton"
        }
    }
]

print json.dumps(info)
