{{- if eq .Values.gateway.className "airlock-microgateway" }}
{{- if .Values.additionalConfig.airlock.createLbService }}
apiVersion: microgateway.airlock.com/v1alpha1
kind: GatewayParameters
metadata:
  name: {{ .Release.Name }}-airlock-gw-params
  namespace: {{ .Release.Namespace }}
spec:
  kubernetes:
    service:
      type: LoadBalancer
{{- end }}
{{- end }}
