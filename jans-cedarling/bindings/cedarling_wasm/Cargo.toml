[package]
name = "cedarling_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Required for WASM output

[dependencies]
wasm-bindgen = "0.2" # Common dependency for WASM interop
