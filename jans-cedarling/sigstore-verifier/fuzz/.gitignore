# Build output — regenerated by `cargo +nightly fuzz run/build`.
target/

# Accumulated fuzz corpus and crash reproducers — grow unbounded and are
# machine/run-specific. Seed inputs live in tests/fixtures/ (symlinked in)
# and stay reproducible from there; nothing here is meant to be permanent.
# Run `cargo +nightly fuzz cmin <target>` first if you want to commit a
# minimized corpus deliberately — see fuzz/README.md.
corpus/
artifacts/
coverage/

# Python bytecode cache from running run_fuzz.py.
__pycache__/
